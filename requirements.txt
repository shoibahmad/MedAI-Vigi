# Production dependencies for PhenoRx.
#
# The scientific pins below MUST match the environment that produced
# models/adr_model.pkl. A numpy 2.x pickle cannot be read by numpy 1.x - loading
# it fails with "PCG64 is not a known BitGenerator module" and the service runs
# permanently degraded with ml_model.loaded = false.
#
# If you retrain the model, regenerate these pins from the training environment.

# Core Web Framework
Flask==3.1.2
Flask-CORS==4.0.1
Werkzeug==3.1.3

# Data Processing & Numerical Computing
# numpy and scipy are pickle-compatibility critical - see note above.
numpy==2.3.4
pandas==2.3.3
scipy==1.16.2

# Machine Learning & Scientific Pipeline
# scikit-learn and joblib are pickle-compatibility critical - see note above.
scikit-learn==1.7.2
joblib==1.5.2

# AI Integration (NVIDIA NIM via the OpenAI-compatible client)
openai==3.8.0

# Production Web Server
gunicorn==23.0.0

# Validation & Schemas
pydantic==2.12.2

# Configuration & Utilities
python-dotenv==1.0.1
pyyaml==6.0.1
python-json-logger==2.0.7
requests==2.32.3
Jinja2==3.1.4
MarkupSafe==2.1.5
